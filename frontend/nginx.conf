server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # /api/* is routed to the backend by Caddy at the host level, not here —
    # this container only ever serves its own static build.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
